---
created: 2025-10-25T15:04:18Z
last_updated: 2025-10-25T15:04:18Z
version: 1.0
author: Claude Code PM System
title: Next.js + TypeScript + Tailwind CSS设置
priority: 2
status: pending
estimated_hours: 12
parallel: true
dependencies: [001]
assignee: frontend-agent
labels: [nextjs, typescript, tailwind, setup, phase-1]
---

# 任务002: Next.js + TypeScript + Tailwind CSS设置

## 任务概述
建立史诗AI项目的前端基础框架，配置Next.js 14、TypeScript严格模式和Tailwind CSS样式系统，为后续UI开发奠定基础。

## 验收标准
- [ ] Next.js 14项目创建并配置完成
- [ ] TypeScript严格模式配置，类型检查通过
- [ ] Tailwind CSS 3.x集成和主题配置
- [ ] 基础页面路由结构建立
- [ ] 响应式布局网格系统配置
- [ ] 开发服务器正常启动，热重载工作正常

## 详细任务

### 1. Next.js项目初始化
```bash
# 创建Next.js 14项目
npx create-next-app@latest epic-ai --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"

# 安装必要依赖
pnpm add @types/node @types/react @types/react-dom
pnpm add -D tailwindcss postcss autoprefixer @tailwindcss/typography
```

### 2. TypeScript配置优化
```json
// tsconfig.json
{
  "compilerOptions": {
    "target": "es5",
    "lib": ["dom", "dom.iterable", "es6"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

### 3. Tailwind CSS配置
```js
// tailwind.config.js
module.exports = {
  content: [
    './src/pages/**/*.{js,ts,jsx,tsx,mdx}',
    './src/components/**/*.{js,ts,jsx,tsx,mdx}',
    './src/app/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        primary: {
          50: '#eff6ff',
          500: '#3b82f6',
          600: '#2563eb',
          700: '#1d4ed8',
        },
        gray: {
          50: '#f9fafb',
          100: '#f3f4f6',
          900: '#111827',
        }
      },
      fontFamily: {
        sans: ['Inter', 'system-ui', 'sans-serif'],
        mono: ['JetBrains Mono', 'monospace'],
      },
      spacing: {
        '18': '4.5rem',
        '88': '22rem',
      },
      animation: {
        'fade-in': 'fadeIn 0.5s ease-in-out',
        'slide-up': 'slideUp 0.3s ease-out',
      },
      keyframes: {
        fadeIn: {
          '0%': { opacity: '0' },
          '100%': { opacity: '1' },
        },
        slideUp: {
          '0%': { transform: 'translateY(10px)', opacity: '0' },
          '100%': { transform: 'translateY(0)', opacity: '1' },
        },
      },
    },
  },
  plugins: [
    require('@tailwindcss/typography'),
  ],
}
```

### 4. 基础页面结构
```
src/app/
├── layout.tsx          # 根布局组件
├── page.tsx            # 首页
├── globals.css         # 全局样式
├── loading.tsx         # 加载组件
├── error.tsx           # 错误边界
├── not-found.tsx       # 404页面
└── (dashboard)/        # 路由分组
    ├── layout.tsx
    └── page.tsx
```

### 5. 基础组件架构
```
src/components/
├── ui/                 # 基础UI组件
│   ├── button.tsx
│   ├── input.tsx
│   ├── card.tsx
│   └── modal.tsx
├── layout/             # 布局组件
│   ├── header.tsx
│   ├── sidebar.tsx
│   └── footer.tsx
└── common/             # 通用组件
    ├── loading-spinner.tsx
    └── error-boundary.tsx
```

## 技术要求

### 环境变量
```env
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_NAME=史诗AI
NEXT_PUBLIC_APP_DESCRIPTION=AI驱动的项目管理平台
```

### 全局样式配置
```css
/* src/app/globals.css */
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  :root {
    --background: 0 0% 100%;
    --foreground: 222.2 84% 4.9%;
    --card: 0 0% 100%;
    --card-foreground: 222.2 84% 4.9%;
  }
}

@layer components {
  .btn-primary {
    @apply bg-primary-600 text-white px-4 py-2 rounded-md hover:bg-primary-700 transition-colors;
  }
}
```

### 响应式断点配置
```js
// 在tailwind.config.js中添加自定义断点
screens: {
  'xs': '475px',
  'sm': '640px',
  'md': '768px',
  'lg': '1024px',
  'xl': '1280px',
  '2xl': '1536px',
}
```

## 实施检查清单

### Next.js配置
- [ ] next.config.js优化配置
- [ ] App Router配置正确
- [ ] 中间件基础配置
- [ ] 重定向规则设置

### TypeScript配置
- [ ] 严格模式启用
- [ ] 路径别名配置
- [ ] 类型定义文件完整
- [ ] 编译无错误无警告

### Tailwind CSS配置
- [ ] PostCSS配置正确
- [ ] 自定义主题配置
- [ ] 响应式设计系统
- [ ] 动画和过渡效果

### 基础页面
- [ ] 根布局组件
- [ ] 首页基础结构
- [ ] 错误页面配置
- [ ] 加载状态组件

### 组件架构
- [ ] 组件目录结构
- [ ] 基础UI组件
- [ ] 布局组件
- [ ] 通用工具组件

## 预期产出物
1. 完整的Next.js + TypeScript项目结构
2. Tailwind CSS样式系统配置
3. 基础页面和组件架构
4. 响应式设计框架
5. 开发环境热重载系统

## 风险与注意事项
- 确保TypeScript严格模式下的类型安全
- Tailwind CSS配置需要考虑性能优化
- App Router的新特性需要正确使用
- 组件设计需要考虑可复用性

## 后续依赖
此任务完成后，可以进行shadcn/ui组件库集成和品牌主题系统实现。