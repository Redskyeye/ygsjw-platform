---
created: 2025-10-25T15:04:18Z
last_updated: 2025-10-25T15:59:00Z
version: 1
author: Claude Code PM System
title: 基础API架构
priority: 7
status: pending
estimated_hours: 16
parallel: true
dependencies: [7, 8]
assignee: backend-agent
labels: [api, rest, typescript, phase-1]
github: https://github.com/Redskyeye/ygsjw-platform/issues/8
---

# 任务007: 基础API架构

## 任务概述
为史诗AI项目建立完整的RESTful API架构，包括API路由设计、中间件配置、数据验证、错误处理、API文档和安全性实现。

## 验收标准
- [ ] RESTful API路由结构完整建立
- [ ] 中间件系统（认证、日志、CORS等）配置
- [ ] 数据验证和序列化系统
- [ ] 统一错误处理和响应格式
- [ ] API文档自动生成（Swagger/OpenAPI）
- [ ] API安全性和速率限制

## 详细任务

### 1. API架构基础配置
```ts
// src/lib/api/types.ts
export interface ApiResponse<T = any> {
  success: boolean
  data?: T
  error?: {
    code: string
    message: string
    details?: any
  }
  meta?: {
    pagination?: {
      page: number
      limit: number
      total: number
      totalPages: number
    }
    timestamp: string
    requestId: string
  }
}

// src/lib/api/response.ts
import type { ApiResponse } from './types'

export class ApiResponseBuilder {
  static success<T>(data: T, meta?: any): ApiResponse<T> {
    return {
      success: true,
      data,
      meta: {
        timestamp: new Date().toISOString(),
        requestId: crypto.randomUUID(),
        ...meta,
      },
    }
  }

  static error(
    code: string,
    message: string,
    statusCode = 500,
    details?: any
  ): ApiResponse {
    return {
      success: false,
      error: {
        code,
        message,
        details,
      },
      meta: {
        timestamp: new Date().toISOString(),
        requestId: crypto.randomUUID(),
      },
    }
  }
}
```

### 2. API中间件系统
```ts
// src/lib/api/middleware/auth.ts
import { NextRequest, NextResponse } from 'next/server'
import { auth } from '@/lib/auth'

export async function withAuth(request: NextRequest) {
  const session = await auth()

  if (!session) {
    return NextResponse.json(
      ApiResponseBuilder.error('UNAUTHORIZED', 'Authentication required', 401),
      { status: 401 }
    )
  }

  return session
}

// src/lib/api/middleware/cors.ts
import { NextRequest, NextResponse } from 'next/server'

export function withCors(request: NextRequest) {
  const origin = request.headers.get('origin')
  const allowedOrigins = [
    'http://localhost:3000',
    'https://epic-ai.com',
    'https://www.epic-ai.com',
  ]

  const corsHeaders = {
    'Access-Control-Allow-Methods': 'GET, POST, PUT, DELETE, OPTIONS',
    'Access-Control-Allow-Headers': 'Content-Type, Authorization',
    'Access-Control-Max-Age': '86400',
  }

  if (allowedOrigins.includes(origin || '')) {
    corsHeaders['Access-Control-Allow-Origin'] = origin!
  }

  if (request.method === 'OPTIONS') {
    return new NextResponse(null, { status: 200, headers: corsHeaders })
  }

  return corsHeaders
}
```

### 3. API路由结构
```
src/app/api/
├── v1/
│   ├── auth/
│   │   ├── signin/
│   │   │   └── route.ts
│   │   ├── signup/
│   │   │   └── route.ts
│   │   └── me/
│   │       └── route.ts
│   ├── users/
│   │   ├── route.ts
│   │   └── [id]/
│   │       └── route.ts
│   ├── projects/
│   │   ├── route.ts
│   │   └── [id]/
│   │       └── route.ts
│   ├── tasks/
│   │   ├── route.ts
│   │   └── [id]/
│   │       └── route.ts
│   ├── ai/
│   │   ├── chat/
│   │   │   └── route.ts
│   │   └── generate/
│   │       └── route.ts
│   └── health/
│       └── route.ts
└── docs/
    └── route.ts
```

### 4. 数据验证Schema
```ts
// src/lib/api/schemas/project.ts
import { z } from 'zod'

export const createProjectSchema = z.object({
  name: z.string().min(1, 'Project name is required').max(100, 'Project name too long'),
  description: z.string().max(500, 'Description too long').optional(),
  priority: z.enum(['LOW', 'MEDIUM', 'HIGH', 'URGENT']).default('MEDIUM'),
  startDate: z.string().datetime().optional(),
  endDate: z.string().datetime().optional(),
  budget: z.number().positive().optional(),
  tags: z.array(z.string()).optional(),
})

export const updateProjectSchema = createProjectSchema.partial().extend({
  status: z.enum(['PLANNING', 'ACTIVE', 'ON_HOLD', 'COMPLETED', 'CANCELLED']).optional(),
})
```

### 5. API路由实现示例
```ts
// src/app/api/v1/projects/route.ts
import { NextRequest, NextResponse } from 'next/server'
import { ApiResponseBuilder } from '@/lib/api/response'
import { withAuth } from '@/lib/api/middleware/auth'
import { withCors } from '@/lib/api/middleware/cors'
import { createProjectSchema } from '@/lib/api/schemas/project'
import { ProjectService } from '@/lib/services/project.service'

export async function GET(request: NextRequest) {
  try {
    // CORS处理
    const corsHeaders = withCors(request)
    if (request.method === 'OPTIONS') {
      return new NextResponse(null, { status: 200, headers: corsHeaders })
    }

    // 认证
    const session = await withAuth(request)
    if (session instanceof NextResponse) return session

    // 获取项目列表
    const projects = await ProjectService.findByUserId(session.user.id)

    return NextResponse.json(
      ApiResponseBuilder.success(projects),
      { headers: corsHeaders }
    )
  } catch (error) {
    console.error('Projects GET error:', error)
    return NextResponse.json(
      ApiResponseBuilder.error('INTERNAL_ERROR', 'Internal server error'),
      { status: 500, headers: withCors(request) }
    )
  }
}

export async function POST(request: NextRequest) {
  try {
    const corsHeaders = withCors(request)
    if (request.method === 'OPTIONS') {
      return new NextResponse(null, { status: 200, headers: corsHeaders })
    }

    const session = await withAuth(request)
    if (session instanceof NextResponse) return session

    const body = await request.json()
    const validatedData = createProjectSchema.parse(body)

    const project = await ProjectService.create({
      ...validatedData,
      owner: { connect: { id: session.user.id } },
    })

    return NextResponse.json(
      ApiResponseBuilder.success(project),
      {
        status: 201,
        headers: corsHeaders,
      }
    )
  } catch (error) {
    console.error('Projects POST error:', error)
    return NextResponse.json(
      ApiResponseBuilder.error('INTERNAL_ERROR', 'Internal server error'),
      { status: 500, headers: withCors(request) }
    )
  }
}
```

### 6. API健康检查
```ts
// src/app/api/v1/health/route.ts
import { NextResponse } from 'next/server'
import { ApiResponseBuilder } from '@/lib/api/response'
import { healthCheck, getDatabaseStats } from '@/lib/db-utils'

export async function GET() {
  try {
    const [dbHealth, dbStats] = await Promise.all([
      healthCheck(),
      getDatabaseStats(),
    ])

    const health = {
      status: 'healthy',
      timestamp: new Date().toISOString(),
      version: process.env.npm_package_version || '1.0.0',
      environment: process.env.NODE_ENV || 'development',
      services: {
        database: dbHealth,
      },
      stats: dbStats,
      uptime: process.uptime(),
    }

    // 如果任何服务不健康，返回503状态
    const isHealthy = Object.values(health.services).every(
      (service: any) => service.status === 'healthy'
    )

    return NextResponse.json(
      ApiResponseBuilder.success(health),
      { status: isHealthy ? 200 : 503 }
    )
  } catch (error) {
    return NextResponse.json(
      ApiResponseBuilder.error('HEALTH_CHECK_FAILED', 'Health check failed'),
      { status: 503 }
    )
  }
}
```

## 实施检查清单

### API架构
- [ ] API响应格式标准化
- [ ] 统一错误处理机制
- [ ] 分页和排序系统
- [ ] API版本控制

### 中间件系统
- [ ] 认证中间件
- [ ] 权限控制中间件
- [ ] 数据验证中间件
- [ ] CORS中间件
- [ ] 速率限制中间件

### 数据验证
- [ ] Zod Schema定义
- [ ] 请求参数验证
- [ ] 响应数据序列化
- [ ] 错误信息本地化

### API路由
- [ ] 用户认证路由
- [ ] 项目管理路由
- [ ] 任务管理路由
- [ ] AI功能路由
- [ ] 健康检查路由

### API文档
- [ ] OpenAPI规范生成
- [ ] Swagger UI集成
- [ ] 接口示例和说明
- [ ] 错误码文档

## 预期产出物
1. 完整的RESTful API架构
2. 中间件和验证系统
3. API文档和规范
4. 安全性和性能优化
5. 错误处理和日志系统

## 风险与注意事项
- API安全性需要严格把控
- 数据验证需要全面覆盖
- 错误处理需要用户友好
- 性能优化需要持续监控

## 后续依赖
此任务完成后，可以进行N8N集成客户端和前端API集成。