---
created: 2025-10-25T15:04:18Z
last_updated: 2025-10-25T15:59:00Z
version: 1
author: Claude Code PM System
title: NextAuth.js认证系统
priority: 5
status: pending
estimated_hours: 14
parallel: true
dependencies: [5, 6]
assignee: auth-agent
labels: [authentication, nextauth, security, phase-1]
github: https://github.com/Redskyeye/ygsjw-platform/issues/7
---

# 任务005: NextAuth.js认证系统

## 任务概述

为史诗AI项目实现完整的用户认证系统，包括用户注册、登录、会话管理、权限控制和安全性配置，使用NextAuth.js v5进行实现。

## 验收标准

- [ ] NextAuth.js v5配置完成，支持多种登录方式
- [ ] 用户注册和登录流程完整实现
- [ ] 会话管理和Token安全配置
- [ ] 权限控制和角色管理系统
- [ ] 安全最佳实践实施（CSRF、密码策略等）
- [ ] 认证状态持久化和同步

## 详细任务

### 1. NextAuth.js环境配置

```bash
# 安装依赖
pnpm add next-auth@beta
pnpm add @auth/prisma-adapter
pnpm add bcryptjs argon2
pnpm add -D @types/bcryptjs

# 环境变量配置
# .env.local
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-super-secret-key-here
AUTH_GOOGLE_ID=your-google-client-id
AUTH_GOOGLE_SECRET=your-google-client-secret
AUTH_GITHUB_ID=your-github-client-id
AUTH_GITHUB_SECRET=your-github-client-secret
```

### 2. NextAuth.js配置

```ts
// src/lib/auth.ts
import { NextAuthConfig } from 'next-auth';
import Google from 'next-auth/providers/google';
import GitHub from 'next-auth/providers/github';
import Credentials from 'next-auth/providers/credentials';
import bcrypt from 'bcryptjs';
import { PrismaAdapter } from '@auth/prisma-adapter';
import { prisma } from '@/lib/prisma';

export const config = {
  adapter: PrismaAdapter(prisma),
  providers: [
    Google({
      clientId: process.env.AUTH_GOOGLE_ID,
      clientSecret: process.env.AUTH_GOOGLE_SECRET,
    }),
    GitHub({
      clientId: process.env.AUTH_GITHUB_ID,
      clientSecret: process.env.AUTH_GITHUB_SECRET,
    }),
    Credentials({
      credentials: {
        email: { label: 'Email', type: 'email' },
        password: { label: 'Password', type: 'password' },
      },
      async authorize(credentials) {
        if (!credentials?.email || !credentials?.password) {
          return null;
        }

        const user = await prisma.user.findUnique({
          where: { email: credentials.email as string },
        });

        if (!user || !user.password) {
          return null;
        }

        const isPasswordValid = await bcrypt.compare(
          credentials.password as string,
          user.password
        );

        if (!isPasswordValid) {
          return null;
        }

        return {
          id: user.id,
          email: user.email,
          name: user.name,
          role: user.role,
        };
      },
    }),
  ],
  callbacks: {
    async session({ session, token }) {
      if (token.sub && session.user) {
        session.user.id = token.sub;
        session.user.role = token.role as string;
      }
      return session;
    },
    async jwt({ token, user }) {
      if (user) {
        token.role = user.role;
      }
      return token;
    },
  },
  session: {
    strategy: 'jwt',
    maxAge: 30 * 24 * 60 * 60, // 30 days
  },
  pages: {
    signIn: '/auth/signin',
    signUp: '/auth/signup',
    error: '/auth/error',
    verifyRequest: '/auth/verify-request',
    newUser: '/auth/new-user',
  },
} satisfies NextAuthConfig;

export const { handlers, auth, signIn, signOut } = NextAuth(config);
```

### 3. API路由配置

```ts
// src/app/api/auth/[...nextauth]/route.ts
import { handlers } from '@/lib/auth';

export const { GET, POST } = handlers;

// src/middleware.ts
import { auth } from '@/lib/auth';

export default auth(req => {
  // 如果用户未认证且不在认证页面，重定向到登录页
  if (!req.auth && !req.nextUrl.pathname.startsWith('/auth')) {
    const newUrl = new URL('/auth/signin', req.nextUrl.origin);
    newUrl.searchParams.set('callbackUrl', req.nextUrl.pathname);
    return Response.redirect(newUrl);
  }
});

export const config = {
  matcher: ['/((?!api|_next/static|_next/image|favicon.ico).*)'],
};
```

### 4. 认证页面组件

```tsx
// src/app/auth/signin/page.tsx
import { Suspense } from 'react';
import { getProviders } from 'next-auth/react';
import { redirect } from 'next/navigation';
import { AuthCard } from '@/components/auth/auth-card';
import { SignInForm } from '@/components/auth/signin-form';
import { SocialLogins } from '@/components/auth/social-logins';

export default async function SignInPage() {
  const providers = await getProviders();

  return (
    <div className="min-h-screen flex items-center justify-center bg-gradient-subtle">
      <AuthCard
        title="欢迎回来"
        description="登录您的史诗AI账户"
        footerText="还没有账户？"
        footerLinkText="立即注册"
        footerHref="/auth/signup"
      >
        <div className="space-y-6">
          <SocialLogins providers={providers} />
          <div className="relative">
            <div className="absolute inset-0 flex items-center">
              <span className="w-full border-t" />
            </div>
            <div className="relative flex justify-center text-xs uppercase">
              <span className="bg-background px-2 text-muted-foreground">
                或者
              </span>
            </div>
          </div>
          <Suspense fallback={<div>Loading...</div>}>
            <SignInForm />
          </Suspense>
        </div>
      </AuthCard>
    </div>
  );
}
```

### 5. 权限控制中间件

```ts
// src/lib/permissions.ts
import { auth } from "@/lib/auth"
import { redirect } from "next/navigation"

export async function requireAuth() {
  const session = await auth()
  if (!session) {
    redirect("/auth/signin")
  }
  return session
}

export async function requireRole(roles: string[]) {
  const session = await requireAuth()
  if (!roles.includes(session.user.role)) {
    redirect("/unauthorized")
  }
  return session
}

// 使用示例
// src/app/dashboard/page.tsx
import { requireAuth } from "@/lib/permissions"
import { DashboardContent } from "@/components/dashboard/dashboard-content"

export default async function DashboardPage() {
  const session = await requireAuth()

  return <DashboardContent user={session.user} />
}
```

## 实施检查清单

### 基础配置

- [ ] NextAuth.js v5安装配置
- [ ] 环境变量设置
- [ ] 数据库模型定义
- [ ] API路由配置

### 认证提供者

- [ ] Google OAuth配置
- [ ] GitHub OAuth配置
- [ ] 邮箱密码认证
- [ ] 社交登录UI组件

### 会话管理

- [ ] JWT配置和安全
- [ ] 会话持久化
- [ ] Token刷新机制
- [ ] 会话状态管理

### 权限控制

- [ ] 角色权限系统
- [ ] 路由保护中间件
- [ ] 组件级权限控制
- [ ] API权限验证

### 安全措施

- [ ] 密码加密存储
- [ ] CSRF保护
- [ ] 会话安全配置
- [ ] 错误处理和日志

## 预期产出物

1. 完整的用户认证系统
2. 多种登录方式支持
3. 安全的会话管理
4. 权限控制和角色系统
5. 认证相关UI组件

## 风险与注意事项

- OAuth配置需要正确的回调URL
- 密码安全需要遵循最佳实践
- 会话管理需要考虑性能和安全
- 权限系统需要灵活可扩展

## 后续依赖

此任务完成后，可以进行数据库集成和基础API架构开发。
