---
created: 2025-10-25T15:04:18Z
last_updated: 2025-10-25T15:59:00Z
version: 1
author: Claude Code PM System
title: Prisma + SQLite数据库
priority: 6
status: pending
estimated_hours: 12
parallel: true
dependencies: [7]
assignee: database-agent
labels: [database, prisma, sqlite, phase-1]
github: https://github.com/Redskyeye/ygsjw-platform/issues/8
---

# 任务006: Prisma + SQLite数据库

## 任务概述

为史诗AI项目建立完整的数据库架构，使用Prisma ORM和SQLite数据库，实现数据模型定义、迁移管理、种子数据和数据库操作封装。

## 验收标准

- [ ] Prisma ORM配置完成，SQLite数据库连接正常
- [ ] 完整的数据模型定义（用户、项目、任务、AI对话等）
- [ ] 数据库迁移和版本管理系统
- [ ] 种子数据和初始数据填充
- [ ] 数据库操作服务层封装
- [ ] 数据库性能优化和索引配置

## 详细任务

### 1. Prisma环境配置

```bash
# 安装依赖
pnpm add prisma @prisma/client
pnpm add -D prisma

# 初始化Prisma
npx prisma init --datasource-provider sqlite

# 配置文件生成
# prisma/schema.prisma
# .env
```

### 2. 数据库配置

```env
# .env
# Database
DATABASE_URL="file:./dev.db"

# Prisma配置
DIRECT_URL="file:./dev.db"
```

### 3. Prisma客户端配置

```ts
// src/lib/prisma.ts
import { PrismaClient } from '@prisma/client';

const globalForPrisma = globalThis as unknown as {
  prisma: PrismaClient | undefined;
};

export const prisma =
  globalForPrisma.prisma ??
  new PrismaClient({
    log: ['query'],
  });

if (process.env.NODE_ENV !== 'production') globalForPrisma.prisma = prisma;

// 数据库连接测试
export async function testDatabaseConnection() {
  try {
    await prisma.$connect();
    console.log('✅ Database connected successfully');
    return true;
  } catch (error) {
    console.error('❌ Database connection failed:', error);
    return false;
  }
}
```

### 4. 数据库迁移管理

```bash
# 生成迁移文件
npx prisma migrate dev --name init

# 重置数据库
npx prisma migrate reset

# 部署迁移
npx prisma migrate deploy

# 生成客户端
npx prisma generate
```

### 5. 种子数据

```ts
// prisma/seed.ts
import { PrismaClient } from '@prisma/client';
import bcrypt from 'bcryptjs';

const prisma = new PrismaClient();

async function main() {
  console.log('🌱 Starting database seeding...');

  // 创建管理员用户
  const adminPassword = await bcrypt.hash('admin123', 10);
  const admin = await prisma.user.upsert({
    where: { email: 'admin@epic-ai.com' },
    update: {},
    create: {
      email: 'admin@epic-ai.com',
      name: '系统管理员',
      password: adminPassword,
      role: 'ADMIN',
    },
  });

  // 创建测试用户
  const userPassword = await bcrypt.hash('user123', 10);
  const testUser = await prisma.user.upsert({
    where: { email: 'user@example.com' },
    update: {},
    create: {
      email: 'user@example.com',
      name: '测试用户',
      password: userPassword,
      role: 'USER',
    },
  });

  console.log('✅ Database seeding completed successfully');
}

main()
  .catch(e => {
    console.error(e);
    process.exit(1);
  })
  .finally(async () => {
    await prisma.$disconnect();
  });
```

### 6. 数据库服务层

```ts
// src/lib/services/user.service.ts
import { prisma } from '@/lib/prisma';
import type { User, Prisma } from '@prisma/client';

export class UserService {
  static async findById(id: string) {
    return prisma.user.findUnique({
      where: { id },
      include: {
        projects: {
          include: {
            members: true,
            tasks: true,
          },
        },
      },
    });
  }

  static async findByEmail(email: string) {
    return prisma.user.findUnique({
      where: { email },
    });
  }

  static async create(data: Prisma.UserCreateInput) {
    return prisma.user.create({
      data,
    });
  }

  static async update(id: string, data: Prisma.UserUpdateInput) {
    return prisma.user.update({
      where: { id },
      data,
    });
  }

  static async delete(id: string) {
    return prisma.user.delete({
      where: { id },
    });
  }
}
```

### 7. 数据库工具函数

```ts
// src/lib/db-utils.ts
import { prisma } from '@/lib/prisma';

// 数据库健康检查
export async function healthCheck() {
  try {
    await prisma.$queryRaw`SELECT 1`;
    return { status: 'healthy', timestamp: new Date().toISOString() };
  } catch (error) {
    return {
      status: 'unhealthy',
      error: error instanceof Error ? error.message : 'Unknown error',
      timestamp: new Date().toISOString(),
    };
  }
}

// 数据库统计
export async function getDatabaseStats() {
  const [userCount, projectCount, taskCount] = await Promise.all([
    prisma.user.count(),
    prisma.project.count(),
    prisma.task.count(),
  ]);

  return {
    users: userCount,
    projects: projectCount,
    tasks: taskCount,
  };
}
```

## 实施检查清单

### 基础配置

- [ ] Prisma ORM安装配置
- [ ] SQLite数据库连接
- [ ] 环境变量配置
- [ ] 数据库客户端初始化

### 数据模型

- [ ] 用户认证模型
- [ ] 项目管理模型
- [ ] 任务管理模型
- [ ] AI对话模型
- [ ] 文档管理模型
- [ ] 通知系统模型

### 迁移管理

- [ ] 初始迁移文件生成
- [ ] 数据库schema同步
- [ ] 迁移版本管理
- [ ] 回滚机制

### 种子数据

- [ ] 管理员账户创建
- [ ] 示例项目数据
- [ ] 系统配置数据
- [ ] 测试数据生成

### 服务层

- [ ] 用户服务封装
- [ ] 项目服务封装
- [ ] 任务服务封装
- [ ] 数据库工具函数

## 预期产出物

1. 完整的数据库架构
2. Prisma ORM配置和客户端
3. 数据迁移和种子数据
4. 数据库服务层封装
5. 数据库管理工具

## 风险与注意事项

- SQLite在生产环境需要考虑并发限制
- 数据模型设计需要考虑扩展性
- 迁移脚本需要谨慎处理生产数据
- 数据库性能需要监控和优化

## 后续依赖

此任务完成后，可以进行基础API架构和N8N集成客户端开发。
